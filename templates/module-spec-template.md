# 模块接口契约

> 由方案设计 Agent 生成。每个模块 Agent 严格按照自己负责的那一段施工。
> 跨模块接口约定必须写到「另一个 Agent 不看这份文档也能写出兼容代码」的程度。

---

## 模块 1: [模块名]
- **负责 Agent**: [agent 标签]
- **文件路径**: [根目录]
- **依赖模块**: [有依赖就写，没有写"无"]

### 文件清单

| 文件路径 | 内容 | 关键定义 |
|----------|------|----------|
| | | |

### 导出接口（供其他模块调用）

| 函数/端点 | 签名 | 输入 | 输出 | 错误情况 |
|-----------|------|------|------|----------|
| | | | | |

### 对模块 X 的接口约定

- [模块名] 调用 [端点] 时传入 [参数格式]
- 返回格式: [JSON 结构]
- 错误时返回: [错误格式]
- [任何特殊约定]

---

## 模块 2: [模块名]
- **负责 Agent**: [agent 标签]
- **文件路径**: [根目录]
- **依赖模块**: [列出依赖的模块]

### 文件清单

| 文件路径 | 内容 | 关键定义 |
|----------|------|----------|
| | | | |

### 对模块 X 的接口依赖

- 调用 [端点] 时期望返回 [格式]
- [类型定义/接口定义]

---

## 跨模块接口一致性检查表

| 模块 A 提供 | 模块 B 调用 | 是否一致 |
|------------|------------|----------|
| | | ✅/❌ |
