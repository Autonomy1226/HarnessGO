# [模块名] — 施工文件

> 由方案设计 Agent 生成。**本模块的 Agent 只读这份文件就能完成所有代码。**
> 不包含任何其他模块的信息——那些信息在本模块 Agent 的工作范围之外。

---

## 模块信息

| 字段 | 值 |
|------|----|
| 模块名 | |
| 负责 Agent 标签 | |
| 根目录 | |
| 依赖模块 | [无 / 依赖模块名] |
| 在 design.md 中的位置 | §1.6 模块 [N] |

---

## 文件清单

> 本模块需要创建/修改的每个文件。

| # | 文件路径 | 内容 | 关键类/函数/类型 |
|---|----------|------|-----------------|
| 1 | | | |
| 2 | | | |

---

## 详细施工要求

### 文件 1: [路径]

```
[精确到函数签名、字段类型、约束条件]
```

### 文件 2: [路径]

```
[精确到函数签名、字段类型、约束条件]
```

---

## 导出接口（供其他模块调用）

| 函数/端点 | 签名 | 输入 | 输出 | 错误情况 |
|-----------|------|------|------|----------|
| | | | | |

---

## 对依赖模块的接口约定

> 如果本模块依赖其他模块，定义本模块期望的接口格式。

| 依赖模块 | 调用的接口 | 期望格式 | 错误时本模块如何处理 |
|----------|-----------|----------|---------------------|
| | | | |

---

## 跨模块接口一致性核对

> 以下由集成检查 Agent 填写。施工 Agent 不填。

| 本模块提供 | 被模块 X 调用 | 是否一致 |
|-----------|-------------|----------|
| | | |
